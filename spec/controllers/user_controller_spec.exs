defmodule Zlack.UserControllerTest do
  #use ESpec

  #alias Zlack.{UserHTTPClient}

  @moduledoc """
    This module contains tests for the UserController module. The functions in UserActions should NOT be called directly. Those tests belong in Zlack.UserActionsTest. 
    Here we test part of the Zlack HTTP API, so we should use Zlack.UserHTTPClient for all requests.
  """

  # test "GET /api/v1/users, :index" do
  # end

  # test "POST /api/v1/users, :create" do
  # end

  # test "GET /api/v1/users/:id, :show" do
  # end

  # test "PATCH or POST /api/v1/users/:id, :update" do
  # end

  # test "DELETE /api/v1/users/:id, :delete" do
  # end

end
